import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1
      },
      checkboxButton: {
        width: 30,
        height: 30,
        borderWidth: 2,
        borderColor: '#66ae7c',
        borderRadius: 5,
        zIndex: 1
      },
})

export default styles